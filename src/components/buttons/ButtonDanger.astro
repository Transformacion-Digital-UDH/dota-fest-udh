---
const { label, idbtn } = Astro.props;
---

<button id={idbtn} class="dota-button-danger"> {label} </button>

<style>
  .dota-button-danger {
    background: linear-gradient(to bottom, #6b211c, #8e2b19);
    border: 1px solid #bc4539;
    color: white;
    font-size: 16px;
    font-weight: bold;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    padding: 12px 24px;
    cursor: pointer;
    transition:
      filter 0.1s linear,
      border 0.1s linear;
    text-shadow: 0px 0px 6px rgba(0, 0, 0, 1);
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.6);
  }

  .dota-button-danger:hover {
    filter: brightness(1.2);
  }

  .dota-button-danger:active {
    filter: brightness(1.4);
    border-color: #501f18;
    transform: translateY(1px);
  }

  .dota-button-default {
    background: linear-gradient(to bottom, #4c4c4c, #2f2f2f);
    border: 1px solid #666666;
    color: #cccccc;
    font-size: 16px;
    font-weight: bold;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    padding: 12px 24px;
    cursor: pointer;
    transition:
      filter 0.1s linear,
      border 0.1s linear,
      color 0.1s linear;
    text-shadow: 0px 0px 6px rgba(0, 0, 0, 1);
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.6);
  }

  .dota-button-default:hover {
    filter: brightness(1.2);
    color: white;
  }

  .dota-button-default:active {
    filter: brightness(1.4);
    border-color: #444444;
    transform: translateY(1px);
  }
</style>
